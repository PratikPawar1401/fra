# Core FastAPI - Essential for API Gateway
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
python-multipart==0.0.6

# HTTP client
httpx==0.25.2
requests==2.31.0

# Configuration
python-dotenv==1.0.0

# Authentication & Security (basic)
passlib[bcrypt]==1.7.4

# File handling
pillow==10.1.0

# Data processing (basic)
pandas==2.1.4
numpy==1.25.2

# AI Pipeline - OCR
unstract-llmwhisperer==2.0.0

# Note: PostgreSQL dependencies removed for now
# We'll add them after PostgreSQL is properly installed
